"""Typed, env-var-aware application configuration.

Populated in Phase 1: pydantic-settings classes mirroring the
llm_provider / mcp_server / vault config sections, loaded from
`.llm-wiki-config`.
"""
