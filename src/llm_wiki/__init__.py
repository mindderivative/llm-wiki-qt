"""llm_wiki — local-first, self-maintaining knowledge base engine.

Pure-Python engine package with zero PySide6 dependency (see
ARCHITECTURE.md, Design Principle 5). Every capability here must be
exercisable via `pytest` and/or the `llm-wiki` CLI (Phase 12) without
opening a window.
"""

__version__ = "0.1.0"
